{
  "name": "weixin-gateway",
  "version": "0.1.0",
  "description": "WeChat personal assistant gateway — QR login, keep-alive, and multi-type message sending via weixin-agent-sdk",
  "main": "index.js",
  "keywords": ["wechat", "weixin", "chatbot", "gateway", "claude", "opencode"],
  "license": "MIT",
  "engines": { "node": ">=18.0.0" },
  "dependencies": {
    "msedge-tts": "^2.0.4",
    "qrcode": "^1.5.4",
    "silk-sdk": "^0.2.2",
    "weixin-agent-sdk": "^0.2.0"
  },
  "peerDependencies": {
    "express": ">=4.0.0"
  },
  "scripts": {
    "test": "jest --forceExit --testTimeout=10000"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
